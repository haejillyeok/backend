syntax = "proto3";

package haejillyeok.agent.internal.v1;

service InternalHealth {
  rpc Ping(PingRequest) returns (PingResponse);
}

message PingRequest {
  string caller = 1;
}

message PingResponse {
  string service = 1;
  string status = 2;
}
